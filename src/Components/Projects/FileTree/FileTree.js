import React, {Component} from 'react';

class FileTree extends Component {
  state = {
    files: []
  }
  render() {
    return(
      <div>
          FileTree component
      </div>
    )
  }
}

export default FileTree;